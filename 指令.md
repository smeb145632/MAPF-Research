# MAPF-Research 常用指令

## run_lifelong.py

运行 MAPF-Research lifelong 的封装脚本。

### 参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `<problem>` | problem 文件名（不含 `.json` 后缀），或绝对路径。脚本会在所有 domain 目录下搜索同名 `.json` 文件 | 必填 |
| `-c, --screen` | 输出详细度：`1`=完整(含 plannerPaths，可用于 PlanViz)，`2`=仅 actualPaths，`3`=仅摘要 | `1` |
| `-s, --sim` | 仿真步数 | `5000` |
| `-o, --output` | 输出文件名 | `output.json` |
| `-n, --plan` | 初始规划时间限制（ms） | `1000` |
| `-p, --pre` | 预处理时间限制（ms） | `30000` |
| `--no-viz` | 跳过自动可视化，只跑完退出 | 否 |
| `--viz-only` | 只启动可视化，不运行 lifelong（用已有的 output.json） | 否 |

### 使用案例

```bash
cd /mnt/f/MAPF/MAPF-Research

# 跑一个小 case，完整输出，自动弹出 PlanViz
python3 run_lifelong.py warehouse_small_10

# 只跑仿真，不出可视化（-c 3）
python3 run_lifelong.py warehouse_small_50 -c 3

# 自定义输出文件名
python3 run_lifelong.py random_100 -o random100_result.json

# 改仿真步数
python3 run_lifelong.py warehouse_small_10 -s 3000

# 已有 output.json，只弹可视化
python3 run_lifelong.py warehouse_small_10 --viz-only

# 绝对路径也可以
python3 run_lifelong.py /mnt/f/MAPF/MAPF-Research/example_problems/warehouse.domain/warehouse_small_10.json
```

### 已有 problem 一览

| 路径 | 文件 |
|------|------|
| `warehouse.domain` | `warehouse_small_10.json`, `warehouse_small_50.json`, `warehouse_small_100.json`, `warehouse_small_200.json`, `warehouse_small_400.json`, `warehouse_small_800.json`, `warehouse_large_200.json` ~ `warehouse_large_8000.json`, `sortation_large_400.json` ~ `sortation_large_8000.json`, `fulfill-example_2500.json` |
| `random.domain` | `random_50.json`, `random_50.json` (不同 map/scenario) |
| `city.domain` | `paris_500.json`, `paris_5000.json` |
| `game.domain` | `brc202d_500.json`, `brc202d_5000.json` |
| `room.domain` | `room-example_150.json` |

完整列表可用 `ls example_problems/*/` 查看。

---

## run_both.py

PlanViz 可视化封装脚本，供 `run_lifelong.py` 内部调用，也可独立使用。

### 参数

```bash
python3 /mnt/f/MAPF/PlanViz/script/run_both.py <output.json> <problem.json 或 .map 文件>
```

### 使用案例

```bash
# 手动可视化 lifelong 的输出
python3 /mnt/f/MAPF/PlanViz/script/run_both.py output.json problem.json

# 也可以直接指定 .map 文件
python3 /mnt/f/MAPF/PlanViz/script/run_both.py output.json maps/warehouse_small.map
```
